# Procedures

Complete ordered procedures live inside each record in `../operations/records.jsonl` under `required_procedure`. Steps remain connected to applicability, conditions, exceptions, documentation, prohibitions, escalation, related procedures, and exact evidence.

Do not split an ordered sequence into disconnected facts when omission or reordering could change the operational result. Driver-facing summaries are a presentation layer and never replace the complete procedure.
