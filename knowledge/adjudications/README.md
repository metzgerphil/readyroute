# Adjudications

`records.json` contains explicit Ready Route approvals, reopenings, and supersessions. It is intentionally empty after the v2 reset. No decision from the archived v1 corpus carries forward automatically.
