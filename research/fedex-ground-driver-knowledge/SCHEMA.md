# v2 authoring contract

The canonical JSON schema files live under `knowledge/schema/`. Authoring records must provide a stable knowledge ID, situation, applicability, rule, procedure where applicable, concise answer, status, and exact source evidence.

Publication requires both an eligible status and complete evidence gates. Missing, ambiguous, conflicting, or potentially outdated material must fail closed.
